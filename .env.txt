# Настройки приложения
PRO_PATH=app/models  # Путь к директории с моделями
PORT=8000           # Порт для запуска приложения
DEBUG=True          # Режим отладки

# Настройки логирования (опционально)
LOG_LEVEL=INFO
LOG_FILE=app.log

# Настройки базы данных (если используется)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

# Настройки API ключей (если используются)
API_KEY=your_api_key
